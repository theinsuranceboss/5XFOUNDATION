[[plugins]]
package = "@netlify/plugin-nextjs"

[build]
command = "npm run build"

[build.environment]
NEXT_BUILD_DIR = ".next"

[functions]
  # Allow more time for initial DB initialization on cold starts
  node_bundler = "nft"
